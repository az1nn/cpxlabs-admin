# Feature Specifications

This directory is the feature-level source of truth for `cpxlabs-admin`.

## Status Legend

- **Implemented (Retrofitted)**: behavior was implemented before Spec Kit adoption; artifacts were reconstructed from merged code, ADRs, tests, and PR history.
- **Draft / Planned / In Progress**: native Spec Kit feature lifecycle.
- **Implemented / Converged**: feature was developed through Spec Kit and converged against its artifacts.

## Historical Backfill

| Spec | Feature | Status |
|---|---|---|
| `001-foundation` | Enterprise starter foundation | Implemented (Retrofitted) |
| `002-design-system-crud` | Design system and customer CRUD | Implemented (Retrofitted) |
| `003-testing-accessibility` | Storybook, accessibility and E2E | Implemented (Retrofitted) |
| `004-http-api-persistence` | Vercel, HTTP API and PostgreSQL | Implemented (Retrofitted) |

These four specs were created during Spec Kit adoption on 2026-09-08. They are historical records, not claims about the development process used originally.

## New Features

New material features MUST be created through Spec Kit rather than by manually copying one of the historical directories. The next feature number is `005`.

See `docs/development/spec-kit.md` and `.specify/memory/constitution.md` before starting new work.
